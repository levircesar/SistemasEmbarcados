PROCESS_NAME(blinkled);

/**
 * @brief eh uma function para ler o status da variavel leds_state
 * @param void
 * @param
 * @param
 * @param
 * @return: uint8_t contendo o valor da variavel leds_state
 */
//bool get_leds_state(bool);
void blinkledfunction(bool);

 